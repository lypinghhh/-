/**
 * Buffer.byteLength
 * Buffer.isBuffer()
 * Buffer.concat()
 */


 /**
  * buf.length  buf的空间长度，不一定等于实际的长度
  * buf.toString('base64')
  * buf.fill()
  * buf.equals()
  * buf.indexOf()
  * buf.copy()
  */